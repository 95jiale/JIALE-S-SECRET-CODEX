def isIsomorphic(s, t):
    """
    :type s: str
    :type t: str
    :rtype: bool
    """
    for x in range(len(s)):
        print(s[x])


answer = isIsomorphic(s="egg", t="add")
print(answer)
